name: cmg_bh
version: 1.0.0
profile: anvil
model-paths:
- models
macro-paths:
- macros
snapshot-paths:
- snapshots
clean-targets:
- target
- dbt_packages
models:
  +schema: main
  +materialized: table
  sample:
    cmg_bh_stg_sample:
      +schema: main
      +materialized: table
  subject:
    cmg_bh_stg_subject:
      +schema: main
      +materialized: table
