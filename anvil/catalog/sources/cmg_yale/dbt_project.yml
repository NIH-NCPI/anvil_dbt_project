name: cmg_yale
version: 1.0.0
profile: anvil
model-paths:
- models
macro-paths:
- macros
snapshot-paths:
- snapshots
clean-targets:
- target
- dbt_packages
models:
  +schema: main
  +materialized: table
  sample:
    cmg_yale_stg_sample:
      +schema: main
      +materialized: table
  subject:
    cmg_yale_stg_subject:
      +schema: main
      +materialized: table
