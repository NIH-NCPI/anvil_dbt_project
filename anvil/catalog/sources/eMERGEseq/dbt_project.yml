name: eMERGEseq
version: 1.0.0
profile: anvil
model-paths:
- models
macro-paths:
- macros
snapshot-paths:
- snapshots
clean-targets:
- target
- dbt_packages
models:
  +schema: main
  +materialized: table
  demographics:
    eMERGEseq_stg_demographics:
      +schema: main
      +materialized: table
  phecode:
    eMERGEseq_stg_phecode:
      +schema: main
      +materialized: table
  sampleattributes:
    eMERGEseq_stg_sampleattributes:
      +schema: main
      +materialized: table
  samplesubjectmapping:
    eMERGEseq_stg_samplesubjectmapping:
      +schema: main
      +materialized: table
  subjectconsent:
    eMERGEseq_stg_subjectconsent:
      +schema: main
      +materialized: table
  bmi:
    eMERGEseq_stg_bmi:
      +schema: main
      +materialized: table
  pedigree:
    eMERGEseq_stg_pedigree:
      +schema: main
      +materialized: table
